# AI

AI is optional and disabled by default. Deterministic entity, graph and evidence workflows remain functional without a model provider. External content is untrusted data, never instructions. Any AI output remains a proposal/inference and cannot establish ownership or mark a finding `VALIDATED` without deterministic evidence and human-controlled validation.
